import toast from "react-hot-toast";

const NotificacionToast =((modo,mensaje) => {
  return (
      <div>
      {/* switch (modo) {
        case "success":
           toast.success(mensaje)
           break
        case "error":   
           toast.error(mensaje)
           break
        default
          toast(mensaje)
        break     
      }; */}   
    </div>
  )
});

export default NotificacionToast